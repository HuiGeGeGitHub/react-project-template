import React from "react";
import { BrowserRouter, Link, Route } from "react-router-dom";

const routes = (
    <BrowserRouter>
        <div>
            <Route path="/"></Route>
        </div>
    </BrowserRouter>
)
